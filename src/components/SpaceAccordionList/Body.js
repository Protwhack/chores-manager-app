import React from 'react';
import { View } from 'react-native';
import { Text } from 'react-native-elements';

const Body = item => {
  return (
    <View>
      <Text>Routines</Text>
    </View>
  );
};

export default Body;
